#include "main.h"

/**
 * mul - this function mulitples two numbers
 * @a: is the first parameter
 * @b: is the second parameter
 * Return: 1 if it is digit
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
